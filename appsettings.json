{
  // These settings are shown as examples of required values but should be placed in a secrets.json file.
  // Even better store your secrets in an Azure Key Vault and access them using Managed Identity.
  // REMEMBER - Never check your actual secrets into source control kids.
  "tenant_id": "< The tenant id of the AAD user being used to authenticate>",
  "client_id": "< The AAD app registration id of the client application requesting the access token >",
  "client_secret": "< The client secret of the client application requesting the access token >",
  "scope": "< The scope of requested for the downstream API the token is intended for >",
  "username": "< The UPN of the test automation user >",
  "password": "< The password for the test automation user >"
}