{
  // These settings are shown as examples of required values but should be placed in a secrets.json file
  // Never check your actual secrets into source control kids.
  "tenant_id": "< The tenant id of the user being used to authenticate>",
  "client_id": "< The app registration id of the client application requesting the access token >",
  "client_secret": "< The client secret of the client application requesting the access token >",
  "scope": "< The scope of requested for the downstream API >",
  "username": "< The UPN of the test automation user >",
  "password": "< The password for the test automation user >"
}